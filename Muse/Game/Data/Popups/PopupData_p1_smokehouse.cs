//USING
using UnityEngine;
//USING
using System;
//USING
using System.Collections;
//USING
using System.Collections.Generic;
//CLASS PopupData_p1_smokehouse : PopupData
public class PopupData_p1_smokehouse : PopupData {
    ///METHOD PopupData_p1_smokehouse
    public PopupData_p1_smokehouse (  ) {
        ///METHOD_BODY_START PopupData_p1_smokehouse
        ///Id p1_smokehouse
        Id = "p1_smokehouse";
        ///Type InfoPopupTemplate
        Type = "InfoPopupTemplate";
        ///Text Henry is in charge of the smokehouse. After the hogs are killed, he cuts up the meat, salts it, then hangs it here to dry. He has to keep a small fire burning all day so the smoke dries the meat and makes it last. The smokehouse is kept locked so that slaves don't steal meat.
        Text = "Henry is in charge of the smokehouse. After the hogs are killed, he cuts up the meat, salts it, then hangs it here to dry. He has to keep a small fire burning all day so the smoke dries the meat and makes it last. The smokehouse is kept locked so that slaves don't steal meat.";
        ///RunCloseFirst false
        RunCloseFirst = false;
        ///METHOD_BODY_END PopupData_p1_smokehouse
    }
}
//CLASS_END PopupData_p1_smokehouse : PopupData
